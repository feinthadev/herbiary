package com.avetharun.herbiary.screens;


import io.wispforest.owo.config.ConfigWrapper;
import io.wispforest.owo.config.ui.ConfigScreen;
import org.jetbrains.annotations.Nullable;

import java.util.Map;

public class ModMenuConfigScreen extends ConfigScreen {
    protected ModMenuConfigScreen(net.minecraft.class_2960 modelId, ConfigWrapper<?> config, net.minecraft.@Nullable class_437 parent) {
        super(modelId, config, parent);
    }
}
